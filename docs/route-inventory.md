# Executable route and authorization inventory

Audit date: 2026-08-20. “Mounted” means registered beneath `/api/v1` in `src/app.ts`; it does not mean the workflow is complete. Health and auth-session behavior has API coverage. Child, parent, and administration groups contain executable handlers but broad emulator/authorization coverage is incomplete. OpenAPI currently omits administration and several parent subresources, so those rows are partial.

| Method | Path(s) | Mounted | Authentication / roles | Relationship and tenant enforcement | Validation | OpenAPI | Tests | Status |
|---|---|---:|---|---|---|---|---|---|
| GET/HEAD | `/`, `/health`, `/api/v1/health` | yes | public | n/a | n/a | versioned health only | API | verified |
| GET/POST | `/api/v1/auth/session` and legacy `/api/auth/session` | yes | revoked-token verification in session service | active, unexpired memberships authoritative; inactive, expired, or malformed rows prevent compatibility fallback | bearer parser | yes (versioned only) | unit + API, including expiry/malformed fallback | verified, legacy alias partial |
| POST | `/api/v1/auth/child-token` and legacy alias | yes | anonymous credential exchange | active credential, organization, membership, participant context | strict Zod | yes | unit + API; no full emulator login | partial |
| GET | `/api/v1/participants/:id` | yes | canonical authenticated role | policy-based self/link/team plus organization | ID parser | yes | authorization/unit + unauthenticated API | partial |
| POST | `/api/v1/points/completions`, `/api/v1/points/adjustments` | yes | child / administrator policy respectively | participant/team/organization and point rules | strict Zod + idempotency header | yes | unit + emulator | partial; generic source completion is not a source workflow |
| GET/PUT/POST/PATCH | `/api/v1/child/today`, `/check-ins/today`, `/check-ins/today/draft`, `/check-ins/today/complete`, `/gratitude`, `/character`, `/character/draft`, `/character/complete` | yes | child | self participant and active program context | body schemas; some cursor parsing ad hoc | yes | schema/context/API only | partial |
| GET/POST | `/api/v1/child/bible`, `/bible/:activityId`, `/bible/:activityId/responses`, `/reading`, `/reading/:assignmentId`, `/reading/:assignmentId/responses` | yes | child | self participant, active quarter, scoped source | body schemas + path IDs | yes | schema/context only | partial |
| GET/POST/PATCH | `/api/v1/child/projects`, `/projects/:projectId`, `/projects/:projectId/milestones`, `/projects/:projectId/updates` | yes | child | self/tenant checks in service | strict bodies + IDs | yes | schema/context only | partial |
| GET/POST | `/api/v1/child/team`, `/special-activities`, `/special-activities/:activityId/submissions`, `/surveys`, `/surveys/:activityId/submissions`, `/points`, `/awards` | yes | child | self/tenant checks in service | body schemas; cursor ad hoc | yes | schema/context only | partial |
| GET | `/api/v1/parent/dashboard`, `/children`, `/children/:childId` | yes | parent active membership | active link + matching participant organization; dashboard bounded to 50 | strict query/path schemas | yes | schemas + limited service + unauth API | partial |
| GET/POST | `/api/v1/parent/observations`, `/observations/:id` | yes | parent active membership | author + active link + organization | strict query/body/path | list/create documented; detail absent | limited service | partial |
| GET/PUT | `/api/v1/parent/character/qualities`, `/character/selections/:childId/:quarterId`, `/character/selections` | yes | parent active membership | active link, quarter and quality organization | strict path/body | absent | no workflow tests | partial |
| GET/POST | `/api/v1/parent/family-activities`, `/family-activities/completions` | yes | parent active membership | list checks active link/activity organization; completion performs no write | child query; completion disabled | yes | no workflow tests | list partial; completion unsafe and disabled with 503 |
| GET/POST | `/api/v1/parent/academic-support/configuration`, `/academic-support/requests`, `/academic-support/requests/:requestId`; compatibility `/support/categories`, `/support/requests`, `/support/requests/:id` | yes | parent active membership | active link + requester + organization | strict query/body/path | primary plus partial compatibility docs | limited service + API auth | partial |
| GET | `/api/v1/parent/reports/:childId`, `/teams/:teamId/progress` | yes | parent active membership | active child link; team must contain linked child and match quarter tenant | path/query IDs | yes | no workflow tests | partial |
| POST/GET/PATCH/DELETE | `/api/v1/administration/organizations`, `/programs`, `/parent-onboarding`, `/participants`, `/participants/:participantId` | yes | service-enforced admin/super_admin | target organization membership/policy checks | strict Zod/IDs | absent | schemas + unauth API | partial |
| POST/PATCH/PUT/DELETE | `/api/v1/administration/teams`, `/teams/:teamId`, `/teams/:teamId/members`, `/teams/:teamId/members/:participantId` | yes | service-enforced admin | organization/resource checks | strict Zod/IDs | absent | schema only | partial |
| POST | `/api/v1/administration/invitations`, `/invitations/:invitationId/accept`, `/invitations/:invitationId/decision` | yes | service policy varies by action | invitation organization and actor checks | strict Zod/IDs | absent | schema only | partial |
| POST/GET/DELETE | `/api/v1/administration/consents`, `/participants/:participantId/consents`, `/participants/:participantId/consents/:policyKey` | yes | parent/admin policy in service | participant relationship/tenant checks | strict Zod/IDs | absent | schema only | partial |
| PUT | `/api/v1/administration/organizations/:organizationId/users/:userId/memberships` | yes | super_admin/service policy | target organization | strict Zod/IDs | absent | role unit only | partial |
| any | `/api/v1/mentor/*`, `/api/v1/observer/*`, `/api/v1/admin/*`, onboarding/team/membership/organization paths outside administration | no | n/a | n/a | n/a | no | no | absent |

The route inventory deliberately does not infer implementation from collection names, domain interfaces, or documentation. “Partial” includes missing full-stack emulator tests, incomplete runtime Firestore parsing, undocumented mounted operations, non-opaque pagination, or unresolved lifecycle policy.
