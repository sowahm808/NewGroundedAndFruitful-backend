import express from 'express';import helmet from 'helmet';import {onRequest} from 'firebase-functions/v2/https';import {logger} from 'firebase-functions';import {AppError,InternalError} from './shared/errors.js';
const app=express();app.use(helmet());app.use(express.json({limit:'32kb'}));app.get('/api/v1/health',(_req,res)=>res.json({data:{status:'ok'}}));app.use((_req,res)=>res.status(404).json({error:{code:'NOT_FOUND',message:'Resource not found.'}}));app.use((error:unknown,req:express.Request,res:express.Response,_next:express.NextFunction)=>{const safe=error instanceof AppError?error:new InternalError();logger.error('request_failed',{requestId:req.header('x-request-id'),path:req.path,code:safe.code,internalError:error instanceof Error?error.name:'unknown'});res.status(safe.status).json({error:{code:safe.code,message:safe.message}});});
export const api=onRequest({region:'us-central1',enforceAppCheck:process.env.APP_CHECK_ENFORCED!=='false',cors:false,timeoutSeconds:30,memory:'256MiB'},app);
